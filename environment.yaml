name: gt_mir
channels:
  - pytorch
  - defaults
dependencies:
  - jupyter
  - matplotlib
  - numpy
  - pip
  - tensorboard
  - pytorch
  - pandas
  - pip:
      - librosa
      - pedalboard
      - torchaudio
      - torch-tb-profiler
